size: {{size}}
pin:
  count: {{pin.count|default(4, true)}}
  color:
    theme: {{pin.theme|default("blue_gray", true)}}
    variant: primary
    hue: {{pin.hue|default(800, true)}}
body:
  color:
    theme: gray
    variant: primary
    hue: 800
