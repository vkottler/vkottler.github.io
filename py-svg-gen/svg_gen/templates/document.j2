<svg version="{{svg.version}}"
     xmlns="{{svg.xmlns}}"
     viewBox="{{svg.viewbox}}">
	<!-- generated by svg-gen v{{version}} -->
{% for element in svg.elements %}
	{{element}}
{% endfor %}
</svg>

